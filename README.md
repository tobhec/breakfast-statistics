# Breakfast Statistics

Work in progress.

Windows: Rtools (4.5) must be installed to compile packages.

Run the following to install dependencies
`writeLines('PATH="${RTOOLS45_HOME}\\usr\\bin;${PATH}"', con = "~/.Renviron")`


